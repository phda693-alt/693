<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_bg"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="14dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/card_bg"
            android:orientation="vertical"
            android:padding="16dp"
            android:layout_marginBottom="14dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Relatorios"
                android:textSize="24sp"
                android:textColor="@color/text_primary"
                android:textStyle="bold" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="indicadores comerciais, operacionais e financeiros"
                android:textSize="12sp"
                android:textColor="@color/text_secondary"
                android:layout_marginTop="4dp" />
        </LinearLayout>

        <Button android:id="@+id/btnRelVendas" style="@style/ReportButton" android:text="Relatorio de Vendas" />
        <Button android:id="@+id/btnRelLucratividade" style="@style/ReportButton" android:text="Lucratividade por Produto" />
        <Button android:id="@+id/btnRelVendedor" style="@style/ReportButton" android:text="Vendas por Vendedor" />
        <Button android:id="@+id/btnRelComissoes" style="@style/ReportButton" android:text="Comissoes por Vendedor" />
        <Button android:id="@+id/btnRelGarcom" style="@style/ReportButton" android:text="Porcentagens por Garcom" />
        <Button android:id="@+id/btnRelTaxasEntregador" style="@style/ReportButton" android:text="Taxas por Entregador" />
        <Button android:id="@+id/btnRelValesCaixa" style="@style/ReportButton" android:text="Vales do Caixa" />
        <Button android:id="@+id/btnRelAuditoria" style="@style/ReportButton" android:text="Acoes dos Usuarios" />
        <Button android:id="@+id/btnRelEntregador" style="@style/ReportButton" android:text="Entregas por Entregador" />
        <Button android:id="@+id/btnRelCliente" style="@style/ReportButton" android:text="Vendas por Cliente" />
        <Button android:id="@+id/btnRelProdutos" style="@style/ReportButton" android:text="Produtos Mais Vendidos" />
        <Button android:id="@+id/btnRelCaixa" style="@style/ReportButton" android:text="Fechamento de Caixa" />
        <Button android:id="@+id/btnRelContasReceber" style="@style/ReportButton" android:text="Contas a Receber" />
    </LinearLayout>
</ScrollView>
